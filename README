a zc.buildout instance to install pythons

virtualenv --distribute .
bin/pip install zc.buildout
bin/buildout

requires curl, g++.

Debian
dpkg-dev (dpkg-architecture, for multiarch)
libz-dev
libssl-dev
